_content:
  properties:
    power:
      type: list[:Ups]
      optional: true

_objects:
  Ups:
    properties:
      capacity:
        type: int
  RackType:
    properties:
      weight:
        type: int
        optional: true
