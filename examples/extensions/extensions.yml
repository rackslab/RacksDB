_content:
  properties:
    power:
      type: list[:Ups]
      optional: true

_objects:
  Ups:
    properties:
      capacity:
        type: ~watts
  RackType:
    properties:
      weight:
        type: int
        optional: true
